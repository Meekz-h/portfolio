/// <reference types="node" resolution-mode="require"/>
import _fs from 'fs/promises';
/**
 *
 */
export declare class InitialProjectFileGenerator {
    private readonly projectRoot;
    private readonly fs;
    private readonly executeWithDebugLogger;
    /**
     * Responsible for copying getting started template to a new project directory
     * fs is injected for testing
     */
    constructor(projectRoot: string, fs?: typeof _fs, executeWithDebugLogger?: (cwd: string, executable: string, args?: string[] | undefined, execa?: typeof import("execa").execa) => Promise<void>);
    /**
     * Copies the template directory to an amplify folder within the projectRoot
     */
    generateInitialProjectFiles: () => Promise<void>;
    private initializeTsConfig;
}
//# sourceMappingURL=initial_project_file_generator.d.ts.map