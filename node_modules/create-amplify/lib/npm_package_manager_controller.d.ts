import { execa as _execa } from 'execa';
import { DependencyType, PackageManagerController } from './package_manager_controller.js';
/**
 *
 */
export declare class NpmPackageManagerController implements PackageManagerController {
    private readonly projectRoot;
    private readonly execa;
    /**
     * Abstraction around npm commands that are needed to initialize a project and install dependencies
     */
    constructor(projectRoot: string, execa?: typeof _execa);
    /**
     * Installs the given package names as devDependencies
     */
    installDependencies: (packageNames: string[], type: DependencyType) => Promise<void>;
}
//# sourceMappingURL=npm_package_manager_controller.d.ts.map